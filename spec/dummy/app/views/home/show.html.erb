<%= form_for @invitation ||= Invitable::Invitation.new, :url => invitable.invitations_path, :remote => true do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.submit 'Submit' %>
<% end %>
